import Section1 from '.';

import type { Meta, StoryObj } from '@storybook/react';

export default {
  title: 'client/Section1',
  component: Section1,
} as Meta<typeof Section1>;

type Story = StoryObj<typeof Section1>;

export const Primary: Story = {};
