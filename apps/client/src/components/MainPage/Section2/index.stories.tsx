import Section2 from '.';

import type { Meta, StoryObj } from '@storybook/react';

const meta: Meta<typeof Section2> = {
  title: 'Client/MainPage/Section2',
  component: Section2,
};

export default meta;

type Story = StoryObj<typeof Section2>;

export const Primary: Story = {};
