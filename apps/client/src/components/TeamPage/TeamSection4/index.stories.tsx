import TeamSection4 from '.';

import type { Meta, StoryObj } from '@storybook/react';

const meta: Meta<typeof TeamSection4> = {
  title: 'Client/TeamPage/Section4',
  component: TeamSection4,
};

export default meta;

type Story = StoryObj<typeof TeamSection4>;

export const Primary: Story = {};
