import TeamSection2 from '.';

import type { Meta, StoryObj } from '@storybook/react';

const meta: Meta<typeof TeamSection2> = {
  title: 'Client/TeamPage/Section2',
  component: TeamSection2,
};

export default meta;

type Story = StoryObj<typeof TeamSection2>;

export const Primary: Story = {};
